# Example:
# --------
# Parser File:
# </absolute/path/to/file> "<regex filter>" "<type>"
#
# Prse a command output:
# <*read_output.sh from this command> "<regex filter>" "<type>" "<statname>"
# cmd "<type>" "<statname>" "/usr/local/bin/myscript.sh arg1 -arg2 --arg3=value3"

/var/log/auth.log ".+sshd.*(Accepted password for|Failed password for).*" "SSH"

#/var/log/messages "MAC.+SRC.+DST.+PROTO.+DPT" "iptables"
/var/log/ufw.log "MAC.+SRC.+DST.+PROTO.+DPT" "iptables"

#*netstat -ltunp ".*" "netstat" "netstat_listen"

#*docker exec -t sta2 grep 404 /usr/local/openresty/nginx/logs/access.log "404" "nginx_access" "sta2-nginx-access"

#cmd "netstat" "netstat_listen" "netstat -ltunp"
cmd "nginx_access" "sta2-nginx-access" "docker exec -t sta2 grep 404 /usr/local/openresty/nginx/logs/access.log"
